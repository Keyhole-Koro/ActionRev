# ローカル開発用 (Air ホットリロード)
FROM golang:1.23

WORKDIR /app

# Air インストール
RUN go install github.com/air-verse/air@latest

# モジュールを先にコピー
COPY go.mod go.sum ./
RUN go mod download

# ソースは docker-compose でマウントするため COPY しない

EXPOSE 8080

CMD ["air", "-c", ".air.toml"]
