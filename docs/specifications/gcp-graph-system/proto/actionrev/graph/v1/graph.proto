syntax = "proto3";

package actionrev.graph.v1;

option go_package = "github.com/actionrev/backend/gen/actionrev/graph/v1;graphv1";

import "actionrev/graph/v1/common.proto";

service GraphService {
  rpc GetGraph(GetGraphRequest) returns (GetGraphResponse);
}

message GetGraphRequest {
  string document_id = 1;
  repeated NodeCategory category_filters = 2;
  repeated int32 level_filters = 3;
  int32 limit = 4;
  string source_filename_filter = 5;
  bool resolve_aliases = 6;
  repeated EdgeType edge_type_filters = 7;
}

message GetGraphResponse {
  Graph graph = 1;
}
